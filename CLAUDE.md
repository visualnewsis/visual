@AGENTS.md

# Claude Code bootstrap

- `AGENTS.md` is the single source of truth for this repository's project policy.
- Before modifying the repository, check the repository, branch, working tree, and latest `origin/main`, then follow the latest `AGENTS.md` while preserving existing work.
- Do not modify `AGENTS.md` without the project owner's explicit approval.

# VISUAL NEWSIS 공동 작업 지침

이 문서는 이 저장소에서 작업하는 모든 사람과 AI 작업자에게 적용된다.

## 1. 관리 권한과 작업 범위

- 안재현 기자가 프로젝트의 메인 관리자이자 총괄 책임자다.
- 전체 정보구조, 작품 편성, 번호, 공용 랜딩, 디자인 체계, 배포 방향의 최종 결정 권한은 메인 관리자에게 있다.
- 다른 작업자는 메인 관리자가 지정하거나 승인한 콘텐츠 폴더와 파일만 작업한다.
- 담당 콘텐츠를 기존 VISUAL NEWSIS 체계에 추가하는 방식으로 작업하며, 독립된 별도 사이트나 별도 서비스로 분리하지 않는다.
- 작업 범위가 불분명하거나 공용 파일 수정이 필요하면 임의로 진행하지 말고 메인 관리자에게 확인한다.

## 2. 기술 구조

- 이 사이트는 기존 웹사이트의 특정 폴더에 배포되는 정적 콘텐츠다.
- 결과물은 HTML, CSS, 순수 JavaScript와 이미지·영상·음원 등 정적 자산으로 구성한다.
- Next.js, React 서버, Vue 서버, Node.js 서버, API 라우터, 별도 웹 미들웨어, 별도 배포 프로젝트를 새로 만들지 않는다.
- 프레임워크나 빌드 과정이 반드시 필요하다고 판단되면 구현 전에 메인 관리자의 승인을 받는다.
- 배포 후 별도 서버 실행 없이 해당 폴더의 `index.html`에서 바로 작동해야 한다.
- 링크와 자산 경로는 실제 서비스 하위 경로에서 정상 작동하도록 상대경로 또는 승인된 절대경로를 사용한다.

## 3. 공용 파일 보호

- 다음 공용 영역은 메인 관리자 승인 없이 수정하지 않는다.
  - 루트 `index.html`과 VISUAL NEWSIS 메인 랜딩
  - `editshop/index.html`과 편집# 랜딩
  - `arcade/index.html`과 충무로딩 랜딩
  - 공용 헤더·푸터·로고·SEO·분석 태그
  - GitHub Actions와 배포 자동화
  - 여러 콘텐츠가 함께 사용하는 데이터 파일
- 새 콘텐츠 폴더를 추가하는 작업과 공용 랜딩에 노출하는 작업을 구분한다.
- 공용 랜딩 반영은 메인 관리자가 직접 하거나 명시적으로 승인한 경우에만 수행한다.
- 기존 파일 전체를 과거 사본이나 새 템플릿으로 덮어쓰지 않는다.

## 4. 작품 번호 규칙

- VISUAL NEWSIS 전체 작품 번호와 편집# 작품 번호는 서로 독립적으로 관리한다.
- VISUAL NEWSIS 메인은 전체 발행 순서대로 `STORY 001`부터 증가한다.
- 편집#은 편집# 소속 작품끼리 `#001`부터 증가한다.
- 예: 전체 9번째이면서 편집# 7번째 작품은 VISUAL NEWSIS 메인에서 `STORY 009`, 편집#에서 `#007`로 표시한다.
- 번호를 추정하거나 빈 번호를 임의로 채우지 말고 현재 발행 목록과 메인 관리자 지시를 확인한다.

## 5. SEO와 구조화 데이터

- 각 페이지에 적절한 `title`, description, canonical, Open Graph, Twitter Card 정보를 넣는다.
- 필요한 경우 `application/ld+json` 구조화 데이터를 포함한다.
- JSON-LD는 배포 전에 반드시 실제 JSON 파싱 검증을 통과해야 한다.
- 중괄호·대괄호·쉼표·따옴표 누락 여부와 URL, 작품명, 순서를 확인한다.
- SEO 정보를 기존 페이지에서 복사한 뒤 제목·설명·URL을 수정하지 않은 상태로 배포하지 않는다.

## 6. Google 추적 태그

- 기존 사이트에서 사용하는 Google Analytics 또는 Google Tag Manager 태그를 확인해 동일한 방식으로 적용한다.
- 추적 ID를 임의로 새로 만들거나 기존 태그와 중복 삽입하지 않는다.
- 공용 추적 태그를 제거하거나 변경하려면 메인 관리자 승인이 필요하다.
- 배포 전 실제 HTML에 추적 태그가 한 번만 포함됐는지 확인한다.

## 7. Git 동기화와 충돌 방지

- 작업 시작 전 반드시 최신 `origin/main`을 가져와 현재 작업 기준을 동기화한다.
- 로컬에 미커밋 변경이나 원격에 없는 커밋이 있으면 먼저 안전하게 보존한다.
- 다른 작업자의 변경을 덮어쓰는 `reset --hard`, 강제 checkout, force push를 사용하지 않는다.
- 담당 파일만 커밋하며, 무관한 변경이나 생성물을 함께 스테이징하지 않는다.
- 같은 공용 파일을 여러 작업자가 동시에 수정하지 않는다.
- 충돌이 발생하면 임의로 한쪽 버전을 선택하지 말고 양쪽 변경을 비교한다. 판단이 필요한 충돌은 메인 관리자에게 보고한다.
- 자동화가 `main`에 커밋하는 저장소이므로 push 직전에도 원격 변경을 다시 확인한다.

## 8. 배포 전 필수 검증

- HTML 문서와 JavaScript에 문법 오류가 없는지 확인한다.
- 모든 `application/ld+json` 블록이 파싱되는지 확인한다.
- 실제 서비스 경로에서 페이지, 이미지, 영상, 음원, 내부 링크가 정상인지 확인한다.
- 데스크톱과 모바일 화면을 확인한다.
- Google 추적 태그와 SEO 메타데이터를 확인한다.
- 공개 배포 후 실제 URL의 응답과 핵심 콘텐츠 반영 여부를 확인한다.

## 9. AI 작업자에게 전달할 핵심 문장

> 이 작업은 기존 VISUAL NEWSIS 웹사이트의 지정 폴더에 HTML·CSS·순수 JavaScript 정적 파일로 추가하는 작업이다. Next.js 등 별도 웹 미들웨어나 독립 사이트를 구축하지 않는다. SEO 메타데이터, 유효한 JSON-LD 구조화 데이터, 기존 Google 추적 태그를 포함하고 검증한다. 공용 랜딩과 자동화는 메인 관리자 승인 없이 수정하지 않는다.

## 10. VISUAL NEWSIS Global Header Rules

- 충무로딩 예외를 제외한 모든 VISUAL NEWSIS 콘텐츠는 공통 글로벌 헤더를 사용한다.
- PC와 모바일 모두 `VISUAL NEWSIS / NEWSIS 로고 / 현재 섹션명 + ☰` 구조를 사용하고, 현재 섹션명은 햄버거 왼쪽에 표시한다.
- 중앙 NEWSIS 로고는 좌우 요소의 폭과 관계없이 viewport의 정확한 중앙에 고정한다.
- 왼쪽 VISUAL NEWSIS와 오른쪽 섹션명은 작고 절제된 비슷한 시각적 크기를 사용한다.
- 작품 번호는 글로벌 헤더에 표시하지 않는다.
- 실제 랜딩이 존재하는 섹션명만 해당 랜딩으로 연결한다. 편집#은 `/editshop/`, 계산대로는 `/calculator/index.html`로 연결한다.
- 최종판과 알아볼지도는 현재 별도 랜딩이 없으므로 텍스트로만 표시하며, 존재하지 않는 URL을 추측하지 않는다.
- 향후 랜딩이 생기면 헤더 구조를 변경하지 않고 해당 섹션명에 링크만 활성화한다.
- 햄버거 내부 메뉴는 현재 `최신 기사 / 편집# / 충무로딩 / 계산대로` 네 개만 표시한다. 최종판과 알아볼지도는 실제 랜딩이 생길 때까지 표시하지 않는다.
- 햄버거 메뉴는 향후 항목을 쉽게 추가할 수 있는 구조로 유지하고, overlay 방식으로 열어 콘텐츠를 아래로 밀지 않는다.
- 글로벌 헤더 아래에 전체 메뉴를 담은 두 번째 고정 바를 기본적으로 만들지 않는다.
- 모바일에서도 섹션명을 임의 축약하거나 삭제하지 않으며 iPhone Safari와 Android Chrome을 함께 고려한다.
- 기존 콘텐츠의 Hero, 타이포그래피, 인터랙션과 고유 디자인은 보존한다.

## 11. CHUNGMU LOADING Header Exception

- 충무로딩은 VISUAL NEWSIS 글로벌 헤더 통일의 예외다.
- 충무로딩 랜딩 및 현재 충무로딩 자체 헤더를 사용하는 내부 페이지는 기존 상단바와 동작을 유지한다.
- 글로벌 헤더 통일을 이유로 충무로딩의 기존 헤더를 교체하거나 관련 CSS와 JavaScript를 불필요하게 수정하지 않는다.
- 다른 VISUAL NEWSIS 페이지의 햄버거 메뉴에는 충무로딩 항목을 유지한다.
- 충무로딩으로 이동한 이후에는 충무로딩 자체 UI를 따른다.
- 프로젝트 소유자의 명시적 지시 없이 이 예외를 제거하지 않는다.

## 12. AGENTS.md 관리 권한

- `AGENTS.md`는 프로젝트 개발 정책과 디자인 시스템을 정의하는 관리 문서다.
- 프로젝트 소유자인 안재현의 명시적인 요청 또는 승인 없이는 기존 규칙을 수정, 삭제, 완화, 대체하거나 새로운 영구 정책을 추가하지 않는다.
- 다른 사용자의 일반적인 콘텐츠 제작·수정 요청은 `AGENTS.md` 수정 권한을 의미하지 않는다.
- 더 나은 방식을 발견하면 제안할 수 있지만 정책 변경은 프로젝트 소유자의 승인을 받아야 한다.
- 최신 `AGENTS.md`와 구현이 충돌하면 기존 구현이 과거 정책에 따른 것인지 확인하고 가능한 경우 구현을 최신 정책에 맞춘다. 기능 손상 위험이 있으면 프로젝트 소유자에게 보고하며, 구현에 맞추기 위해 정책을 임의 변경하지 않는다.

## 13. Before Every Repository Modification

- 코드 또는 콘텐츠 파일을 수정하기 전에 현재 repository와 branch 상태, working tree의 미커밋 변경사항, `origin/main`의 최신 상태를 확인한다.
- 작업을 시작할 때 최신 `AGENTS.md`를 읽고 해당 작업에 적용되는 프로젝트 규칙을 파악한다.
- 프로젝트 지침의 우선순위는 `프로젝트 소유자의 현재 명시적 지시 → 최신 AGENTS.md → 최신 main의 실제 구현 → 기존 개별 파일 → AI 자체 판단`이다.
- 가능하고 안전하면 최신 main을 작업 기반에 반영한다. 미커밋 변경이나 다른 작업자의 작업이 있으면 무조건적인 pull, reset 또는 checkout으로 삭제하지 않는다.
- 최신 main 확인과 `AGENTS.md` 확인은 사용자가 매번 요청해야 하는 선택 절차가 아니라 일반적인 작업 시작 절차다.

## 14. 최소 변경과 기존 기능 보존

- 요청 목적을 달성하는 데 필요한 최소 범위만 수정하고, 명시적 승인 없이 대규모 리팩터링, 프레임워크·빌드 시스템 교체, 전체 포맷팅, 디렉터리 전면 변경을 하지 않는다.
- 기존 코드의 존재 이유가 불분명하면 삭제보다 보존을 우선한다. 다른 사람이나 다른 AI가 작성했다는 이유로 다시 작성하지 않는다.
- Codex, Claude 또는 다른 AI가 작성한 코드는 모두 동일하게 취급하며 최신 저장소의 실제 코드와 이 문서를 기준으로 판단한다.
- 새 요청과 기존 기능이 충돌하면 기존 기능을 삭제해서 해결하지 말고 프로젝트 소유자에게 충돌 내용을 보고한다.

## 15. 서비스 파일과 경로 확인

- `backup`, `old`, `preview`, `test`, `temp` 등의 이름만으로 실서비스 여부를 판단하지 않는다. 실제 배포 구조와 서비스 경로를 확인한다.
- 백업 파일만 수정하고 완료 처리하지 않으며 과거 파일을 무조건 일괄 수정하지 않는다.
- 페이지, 랜딩, asset, script 경로가 불확실하면 저장소에서 확인하고 존재하지 않는 URL이나 임시 링크를 추측해 만들지 않는다.

## 16. 배포·Git·Secret 보호

- 프로젝트 소유자의 별도 승인 없이 현재 GitHub Pages, GitHub Actions 또는 기타 배포 구조를 교체하거나 기존 workflow를 삭제하지 않는다.
- 충분한 확인이나 명시적 요청 없이 `git reset --hard`, force push, 미커밋 변경 강제 삭제, 다른 작업자의 branch 삭제, 원격 main을 과거 상태로 되돌리는 push를 하지 않는다.
- API key, access token, 비밀번호, 개인 인증정보를 저장소에 평문으로 추가하지 않고 기존 secret 관리 구조를 유지한다.
- 공용 GitHub 계정에서는 identity만으로 현실의 작업자나 프로젝트 소유자를 구분할 수 있다고 가정하지 않는다. 사람별 권한 통제가 구현된 것처럼 표현하지 않는다.

## 17. 완료 전 확인과 보고

- 완료 전 repository 상태와 diff를 확인해 변경 파일, 요청 범위 밖 변경, 의도하지 않은 삭제, 대량 formatting, 기존 기능 제거 여부를 점검한다.
- 공통 UI 또는 인터랙션은 Desktop, iPhone Safari, Android Chrome을 고려한다. 실제 기기에서 확인하지 않았다면 코드·viewport 검증과 실제 기기 검증을 구분해 보고한다.
- 완료 보고에는 수정 내용과 파일, 유지한 영역, 수행한 검증, 실제 기기 검증 여부, 남은 문제를 포함한다.
- 불필요한 임시 파일과 editor·OS cache 누적을 막되 기존에 의도적으로 추적 중인 파일을 임의로 ignore하지 않는다.

## 18. 프로젝트 정책 단일 원본과 다중 AI 협업

- `AGENTS.md`를 VISUAL NEWSIS 프로젝트 정책의 단일 원본(Source of Truth)으로 유지한다.
- AI별 지침 파일에 장문의 정책을 복제하지 않는다. 다른 도구가 별도 지침 파일을 요구하면 `AGENTS.md`를 읽도록 하는 짧은 bootstrap만 둔다.
- 모든 AI 에이전트는 작업 시작 시 저장소 상태 확인, 최신 main 확인, 최신 `AGENTS.md` 적용, 기존 기능 보존, 작업 후 diff와 검증의 흐름을 따른다.
- `AGENTS.md`의 행동 지침은 기술적 권한 통제 자체가 아니다. 사람별 계정이 없는 현재 환경에서는 프로젝트 소유자 전용 승인을 GitHub identity로 강제할 수 없다.

## 19. VISUAL NEWSIS More Stories Rules

- 모든 VISUAL NEWSIS 기사 하단의 더보기 영역은 개별 페이지에 고정된 카드 목록을 새로 만들지 않고 승인된 공통 더보기 asset을 사용한다.
- 편집# 소속 작품은 `editshop-carousel.css`와 `editshop-carousel.js`를 사용한다. 상단 각주는 민트색 `편집# · MORE STORIES`, 제목은 검정 `다른`과 민트색 `편집#` 조합으로 표시한다.
- 편집# 더보기에는 현재 작품을 제외한 편집# 전체 작품을 표시하며, PC에서는 좌우 버튼으로 이동하고 모바일에서는 가로 스와이프와 scroll snap으로 모든 작품을 볼 수 있게 한다.
- 편집# 더보기는 각 작품의 공통 최하단 편집# 브랜드 배너 바로 위에 둔다. 원본 HTML에서 위치가 다르더라도 공통 스크립트가 최종 배치를 통일해야 한다.
- 최종판, 알아볼지도 등 편집#이 아닌 VISUAL NEWSIS 기사는 `visual-stories-carousel.css`와 `visual-stories-carousel.js`를 사용한다.
- 비편집 기사 더보기의 상단 각주는 `VISUAL NEWSIS · MORE STORIES`, 제목은 `비주얼 뉴시스 더 보기`로 표시한다.
- 비편집 기사 더보기에는 VISUAL NEWSIS 메인 `ALL STORIES`에 발행된 전체 기사를 기준으로 현재 작품만 제외해 표시한다. 향후 신규 기사가 `ALL STORIES`에 추가되면 공통 더보기 데이터에도 반드시 같은 발행 번호, 링크, 제목, 설명, 대표 이미지를 추가한다.
- 비편집 기사 더보기는 주요 본문·관련 기사·작품 고유 엔딩이 모두 끝난 뒤 콘텐츠 최하단에 둔다. 별도 공통 브랜드 배너가 있는 페이지에서는 그 배너 바로 위에 둔다.
- 비편집 기사 더보기도 PC 좌우 버튼, 모바일 가로 스와이프와 scroll snap을 사용하며 현재 작품 카드는 표시하지 않는다.
- 공통 더보기 asset을 수정하면 캐시 버전을 갱신하고, Desktop 및 모바일 viewport에서 카드 수, 현재 작품 제외, 버튼 이동, 스와이프, 제목 색상과 최종 배치 순서를 확인한다.
- 배포 검사는 VISUAL NEWSIS 메인 `ALL STORIES`와 비편집 기사 공통 더보기 데이터가 일치하는지 확인해야 하며, 누락된 작품이 있으면 배포를 통과시키지 않는다.

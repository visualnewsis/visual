# VISUAL NEWSIS 공동 작업 지침

이 문서는 이 저장소에서 작업하는 모든 사람과 AI 작업자에게 적용된다.

## 1. 관리 권한과 작업 범위

- 안재현 기자가 프로젝트의 메인 관리자이자 총괄 책임자다.
- 전체 정보구조, 작품 편성, 번호, 공용 랜딩, 디자인 체계, 배포 방향의 최종 결정 권한은 메인 관리자에게 있다.
- 다른 작업자는 메인 관리자가 지정하거나 승인한 콘텐츠 폴더와 파일만 작업한다.
- 담당 콘텐츠를 기존 VISUAL NEWSIS 체계에 추가하는 방식으로 작업하며, 독립된 별도 사이트나 별도 서비스로 분리하지 않는다.
- 작업 범위가 불분명하거나 공용 파일 수정이 필요하면 임의로 진행하지 말고 메인 관리자에게 확인한다.

## 2. 기술 구조

- 이 사이트는 기존 웹사이트의 특정 폴더에 배포되는 정적 콘텐츠다.
- 결과물은 HTML, CSS, 순수 JavaScript와 이미지·영상·음원 등 정적 자산으로 구성한다.
- Next.js, React 서버, Vue 서버, Node.js 서버, API 라우터, 별도 웹 미들웨어, 별도 배포 프로젝트를 새로 만들지 않는다.
- 프레임워크나 빌드 과정이 반드시 필요하다고 판단되면 구현 전에 메인 관리자의 승인을 받는다.
- 배포 후 별도 서버 실행 없이 해당 폴더의 `index.html`에서 바로 작동해야 한다.
- 링크와 자산 경로는 실제 서비스 하위 경로에서 정상 작동하도록 상대경로 또는 승인된 절대경로를 사용한다.

## 3. 공용 파일 보호

- 다음 공용 영역은 메인 관리자 승인 없이 수정하지 않는다.
  - 루트 `index.html`과 VISUAL NEWSIS 메인 랜딩
  - `editshop/index.html`과 편집# 랜딩
  - `arcade/index.html`과 충무로딩 랜딩
  - 공용 헤더·푸터·로고·SEO·분석 태그
  - GitHub Actions와 배포 자동화
  - 여러 콘텐츠가 함께 사용하는 데이터 파일
- 새 콘텐츠 폴더를 추가하는 작업과 공용 랜딩에 노출하는 작업을 구분한다.
- 공용 랜딩 반영은 메인 관리자가 직접 하거나 명시적으로 승인한 경우에만 수행한다.
- 기존 파일 전체를 과거 사본이나 새 템플릿으로 덮어쓰지 않는다.

## 4. 작품 번호 규칙

- VISUAL NEWSIS 전체 작품 번호와 편집# 작품 번호는 서로 독립적으로 관리한다.
- VISUAL NEWSIS 메인은 전체 발행 순서대로 `STORY 001`부터 증가한다.
- 편집#은 편집# 소속 작품끼리 `#001`부터 증가한다.
- 예: 전체 9번째이면서 편집# 7번째 작품은 VISUAL NEWSIS 메인에서 `STORY 009`, 편집#에서 `#007`로 표시한다.
- 번호를 추정하거나 빈 번호를 임의로 채우지 말고 현재 발행 목록과 메인 관리자 지시를 확인한다.

## 5. SEO와 구조화 데이터

- 각 페이지에 적절한 `title`, description, canonical, Open Graph, Twitter Card 정보를 넣는다.
- 필요한 경우 `application/ld+json` 구조화 데이터를 포함한다.
- JSON-LD는 배포 전에 반드시 실제 JSON 파싱 검증을 통과해야 한다.
- 중괄호·대괄호·쉼표·따옴표 누락 여부와 URL, 작품명, 순서를 확인한다.
- SEO 정보를 기존 페이지에서 복사한 뒤 제목·설명·URL을 수정하지 않은 상태로 배포하지 않는다.

## 6. Google 추적 태그

- 기존 사이트에서 사용하는 Google Analytics 또는 Google Tag Manager 태그를 확인해 동일한 방식으로 적용한다.
- 추적 ID를 임의로 새로 만들거나 기존 태그와 중복 삽입하지 않는다.
- 공용 추적 태그를 제거하거나 변경하려면 메인 관리자 승인이 필요하다.
- 배포 전 실제 HTML에 추적 태그가 한 번만 포함됐는지 확인한다.

## 7. Git 동기화와 충돌 방지

- 작업 시작 전 반드시 최신 `origin/main`을 가져와 현재 작업 기준을 동기화한다.
- 로컬에 미커밋 변경이나 원격에 없는 커밋이 있으면 먼저 안전하게 보존한다.
- 다른 작업자의 변경을 덮어쓰는 `reset --hard`, 강제 checkout, force push를 사용하지 않는다.
- 담당 파일만 커밋하며, 무관한 변경이나 생성물을 함께 스테이징하지 않는다.
- 같은 공용 파일을 여러 작업자가 동시에 수정하지 않는다.
- 충돌이 발생하면 임의로 한쪽 버전을 선택하지 말고 양쪽 변경을 비교한다. 판단이 필요한 충돌은 메인 관리자에게 보고한다.
- 자동화가 `main`에 커밋하는 저장소이므로 push 직전에도 원격 변경을 다시 확인한다.

## 8. 배포 전 필수 검증

- HTML 문서와 JavaScript에 문법 오류가 없는지 확인한다.
- 모든 `application/ld+json` 블록이 파싱되는지 확인한다.
- 실제 서비스 경로에서 페이지, 이미지, 영상, 음원, 내부 링크가 정상인지 확인한다.
- 데스크톱과 모바일 화면을 확인한다.
- Google 추적 태그와 SEO 메타데이터를 확인한다.
- 공개 배포 후 실제 URL의 응답과 핵심 콘텐츠 반영 여부를 확인한다.

## 9. AI 작업자에게 전달할 핵심 문장

> 이 작업은 기존 VISUAL NEWSIS 웹사이트의 지정 폴더에 HTML·CSS·순수 JavaScript 정적 파일로 추가하는 작업이다. Next.js 등 별도 웹 미들웨어나 독립 사이트를 구축하지 않는다. SEO 메타데이터, 유효한 JSON-LD 구조화 데이터, 기존 Google 추적 태그를 포함하고 검증한다. 공용 랜딩과 자동화는 메인 관리자 승인 없이 수정하지 않는다.
